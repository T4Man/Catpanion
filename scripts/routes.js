// page('/', startPage.index);

// page('/adoption',
  // );
  //shelterView.index);

// page('/donate')
  //initMap(),
  //navController.index);

// page('/clinics')
  //initMap(),
  //navController.index);

// page('/nanny')
  //initMap(),
  //navController.index);

// page('/Cat-Selector',
  // selectorView.index);

// page('/allBreeds',
  //showAllCats(),
  // CatConstr.index);

// page('/filterCharacteristics',
  // catArticlesController.index);

// page();
